<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Garment - <%= garment.name %></title>
  </head>
  <body>
    <h1>Edit Garments</h1>
    <form action="/garments/<%= garment.id %>?_method=PUT" method="post">
      <label for="name">Name</label>
      <input type="text" name="name" value="<%= garment.name %>" />
      <label for="location">Location</label>
      <input type="text" name="location" value="<%= garment.location %>" />
      <label for="contact">Contact</label>
      <input type="number" name="contact" value="<%= garment.contact %>" />
      <button type="submit">Save</button>
    </form>
  </body>
</html>
